# v0.1.1 发布产物

本目录包含 v0.1.1 的完整 Windows 安装包、模型更新包、桌面端模型注册表、三份 Stage 1 V3 兼容 ONNX 模型，以及 SHA-256 清单。

模型：V3 B6C128（G150）、V3 B8C192（G268）和 V3 B10C256（G258）。旧 `v2.1_high` 不再随本版本发布。

Windows x64 安装包：`Connect4 3D CubeSprite_0.1.1_x64-setup.exe`。
